'use strict';

angular.module('infoImpression')
    .component('infoImpression', {
        bindings: {
            impression: '<'
        },
        templateUrl: 'info-impression/info-impression.template.html',
        controller: infoImpressionController
    });

infoImpressionController.$inject = [];

function infoImpressionController() {
    var ctrl = this;


    ////////////////////

}
